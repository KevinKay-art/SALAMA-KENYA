services:
  - type: web
    name: usalama-kenya
    env: node
    plan: free
    buildCommand: bun install && bun run build
    startCommand: node dist/server.cjs
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
